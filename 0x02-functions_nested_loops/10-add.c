#include"main.h"

/**
 * add- This function adds two integers and returns the resule
 * Return: The addition of the two integers
 * @a: input number for addition
 * @b: input number for addition
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}

