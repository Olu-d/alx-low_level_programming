#include<stdio.h>

int main(void)
{
	int num;
	int ;
	int mul;

	for(num = 0; num <= 9; first_num++)
	{
		while
		{
		counter <= 9;
		mul = first_num + first_num;
		counter++;
		}

		printf("%d", mul);
		putchar(',');
		putchar(' ');
	}

	return (0);



}
