#include"main.h"
#include<stdio.h>

/**
 *_strlen- return the length of an input string
 *@s: input string
 *Return: the value length of the string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
	}

	return (i);


}
